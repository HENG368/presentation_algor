#ifndef tree
#define tree
#include<vector>
#include<string>

using namespace std;
struct condidate{
    string Name;
    uniform_real_distribution<> dist(0.0, 1.0); // 0 to 1
    double x = dist(gen);

    
}
vector<
 




#endif